server {
    listen 80;
    server_name _;

    location = / {
        return 302 /docs/;
    }

    location /docs/ {
        alias /usr/share/nginx/html/;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
