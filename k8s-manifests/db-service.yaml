apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    io.kompose.service: db
spec:
  selector:
    io.kompose.service: db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
