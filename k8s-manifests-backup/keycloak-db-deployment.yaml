apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: keycloak-db
  name: keycloak-db
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: keycloak-db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.service: keycloak-db
    spec:
      containers:
        - name: keycloak-db
          image: postgres:15-alpine
          imagePullPolicy: IfNotPresent
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: fireguard-secrets
                  key: KC_POSTGRES_DB
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: fireguard-secrets
                  key: KC_POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: fireguard-secrets
                  key: KC_POSTGRES_PASSWORD
          ports:
            - containerPort: 5432
              protocol: TCP
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: keycloak-data-dev
      restartPolicy: Always
      volumes:
        - name: keycloak-data-dev
          persistentVolumeClaim:
            claimName: keycloak-data-dev
